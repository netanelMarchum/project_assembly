#include <stdio.h>


/*
    The function reads file content and draws an AM file after layout if there are errors prints them and does not generate a file
*/
int preprocessing(FILE *fd, char* name_file);

